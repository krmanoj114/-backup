package com.tpex.barcode.service;

import org.springframework.stereotype.Component;

@Component
public class TpexBarcodeProducer {

	/*
	 * private static final String TOPIC_NAME = "barcode-topic"; private static
	 * final String TOPIC_FOR_STATUS = "barcode-topic-status"; final Logger log =
	 * LoggerFactory.getLogger(TpexBarcodeProducer.class);
	 * 
	 * @Autowired private JmsTemplate jmsTemplate;
	 * 
	 * public void sendMessage(String barcodeData) { try {
	 * jmsTemplate.convertAndSend(TOPIC_NAME, barcodeData); } catch (JmsException
	 * ex) { log.error(ex.getMessage()); } }
	 * 
	 * public void sendStatusToTopic(String status) {
	 * jmsTemplate.convertAndSend(TOPIC_FOR_STATUS, status); }
	 */

}
